#!/usr/bin/env nu

use ../config.nu *

# Clean up old generations (keep the 2 latest).
print $"Cleaning up old generations \(keeping the ($generations_to_keep) latest\)..."
^sudo -H nh clean all --keep $generations_to_keep --keep-one
